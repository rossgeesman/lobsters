<%= t('views.ban_notification.notify.banned_from', application_name: Rails.application.name, username: @banner.username) %>

  <%= word_wrap(@reason, :line_width => 72).gsub(/\n/, "\n  ") %>

<%= t('views.ban_notification.notify.description' %>
