<%= t('views.email_reply.mention.username', username: @comment.user.username) %>

  <%= word_wrap(@comment.plaintext_comment, :line_width => 72).gsub(/\n/, "\n  ") %>

<%= t('views.email_reply.mention.continue', comment_url: @comment.url) %>
